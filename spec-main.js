describe("InvestmentTerms Constructor ", function () {
	it("should create 'x' props. and 'x' methods", function () {
		// how to test for three props and five methods?
		expect(true);
	});
});
describe("Vehicle Constructor ", function () {
	it("should create 'x' props. and 'x' methods", function () {
		expect(true);
	});
});
describe("Netflix Constructor ", function () {
	it("should create 'x' props. and 'x' methods", function () {
		expect(true);
	});
});